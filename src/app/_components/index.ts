// This is just a helper file that makes it easier for devs trying to consume
// components as the ui-kit becomes more mature. All components can be imported
// from the same place instead of having to import every component from each
// individual file.

export { default as Input } from './Input'
export { default as Button } from './Button'
export { default as Header } from './Header'
export { default as Results } from './Results'